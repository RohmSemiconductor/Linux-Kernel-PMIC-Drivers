#This file is not yet used. I just thought it would be good idea to allow
#specifying some of the target differencies from config file instead of
#hard-coding everything in the scripts.

#For example following ...

#I think it might be doable to use ethernet switch and connect many BBBs
#to the Jenkins slave via it. The first one connected to BD71847 break-out
#board has this IP. Rest of the targets can hae different IPs

TGTLOGDIR=/home/mvaittin/nfs

export TARGET_IP=192.168.255.2


export LOGS="$LOGS $TGTLOGDIR/testlogs/bd71815_volt.log $TGTLOGDIR/testlogs/bd71815_test_log.txt" 
export POWER_PORT=2
